spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS staff\;CREATE SCHEMA IF NOT EXISTS guest\;CREATE SCHEMA IF NOT EXISTS owner\;CREATE SCHEMA IF NOT EXISTS admin\;CREATE SCHEMA IF NOT EXISTS app_auth
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: never
  flyway:
    enabled: false
  mail:
    username: test@example.com

b4code:
  security:
    jwt:
      secret: "thisismytestsecretwhichmustbeatleast32characterslongforthehs256algorithm"
      expirationMs: 86400000
      refreshExpirationMs: 172800000

payhere:
  merchant-id: dummy
  merchant-secret: dummy
  checkout-url: http://dummy
